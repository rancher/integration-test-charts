apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wooowweeee.fullname" . }}-test-connection"
  labels:
{{ include "wooowweeee.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "wooowweeee.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
