apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "welpwelp.fullname" . }}-test-connection"
  labels:
{{ include "welpwelp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "welpwelp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
