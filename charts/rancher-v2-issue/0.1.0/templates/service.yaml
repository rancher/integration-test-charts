apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-v2-issue.fullname" . }}
  labels:
{{ include "rancher-v2-issue.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "rancher-v2-issue.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
