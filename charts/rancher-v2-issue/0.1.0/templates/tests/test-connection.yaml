apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rancher-v2-issue.fullname" . }}-test-connection"
  labels:
{{ include "rancher-v2-issue.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rancher-v2-issue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
